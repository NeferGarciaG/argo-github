apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc-c
spec:
  replicas: 1
  selector:
    matchLabels:
      app: svc-c
  template:
    metadata:
      labels:
        app: svc-c
    spec:
      containers:
      - name: svc-c
        image: "nggarciag/workshop:latest"
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "16Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
