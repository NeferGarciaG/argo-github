apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc-d
spec:
  replicas: 1
  selector:
    matchLabels:
      app: svc-d
  template:
    metadata:
      labels:
        app: svc-d
    spec:
      containers:
      - name: svc-d
        image: "nggarciag/workshop:latest"
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "16Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
